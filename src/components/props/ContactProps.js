import { images } from "../../config/images";

export const ContactProps = {
  contactBannerProps: {
    ContactCardData: [
      {
        src: images.Email,
      },
      {
        src: images.Contact,
      },
    ],
  },
};
